---
import { cvSections } from '../data/cv.js';
---
{cvSections.map((s) => (
  <section class="cv-section">
    <h2 class="sec-label">{s.en}</h2>
    {s.note && <p class="cv-note">{s.note}</p>}
    {s.items.map((it) => (
      <div class="cv-row">
        <div class="cv-label">{it.label}</div>
        <div class="cv-body" set:html={it.html} />
      </div>
    ))}
  </section>
))}
